import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
 
// Standard Vite + React setup. Vite reads VITE_* vars from .env files at build
// time, which is how the app finds your deployed backends in production.
export default defineConfig({
  plugins: [react()],
  server: { port: 5173 },
});
 
